<li>	<%= gravatar_for user, :size => 30 %>	<%= link_to user.name, user %>	<% if current_user.admin? %>	| <%= link_to "sterge", user, :method => :delete, :confirm => "Esti sigur?",									:title => "sterge #{user.name}" %>	<% end %></li>