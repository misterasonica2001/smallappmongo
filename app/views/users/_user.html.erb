<li>	<%= gravatar_for user, :size => 30 %>	<%= link_to user.name, user %>	<% if current_user.admin? %>	| <%= link_to "șterge", user, :method => :delete, :confirm => "Ești sigur?",									:title => "Șterge #{user.name}" %>	<% end %></li>