<% if object.errors.any? %>
	<div id="error_explanation">
		<h2><%= pluralize(object.errors.count, "eroare") %>
			au impiedicat <%= object.class.to_s.underscore.humanize.downcase %> salvarea:
		</h2>
		<p>Exista probleme la urmatoarele campuri:</p>
		<ul>
			<% object.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>